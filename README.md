PBX Nursing Premium V12

Updates:
- Retains V11 bulb motion and navy handwritten bulb callouts.
- Readiness graph uses the supplied 63 -> 94 progression:
  63, 65, 70, 69, 73, 80, 78, 84, 88, 94.
- Graph animation plays once when the analytics panel enters the viewport.
- It does not loop while visible.
- After the panel fully leaves the viewport, it resets and plays once again on the next return.

- V14: changed the entire bulb outline, rays, filament, base, labels, and arrows to navy blue.

- V15: Clinical Judgment now follows the supplied solar-system reference: 34-second clockwise orbit, reference starting angles, fixed center text, and independently orbiting dust dots.

- V16: fixed malformed V15 CSS, equalized all six Clinical Judgment nodes to exact 60° spacing, constrained the ellipse to keep cards fully visible, and increased card/text contrast.

- V17: made the momentum/typewriter text static and changed it to navy blue.

- V18: restored the prior typewriter typing/erasing loop, kept it navy, and fixed both type rows so their screen position never shifts.

- V19: tablet and result card now use the same parallax movement as the Review mode / Question + explanation callout while preserving their 3D tilt.

- V20: tightened vertical whitespace only. Content, colors, animations, behavior, and section order are unchanged.

- V21: added working Policies and Company footer navigation with local Terms, Privacy, Return & Refund, Cancellation, Disclaimer, and About Us pages, plus 2026 copyright.

- V23: footer links open in same-page popups; Explore/Policies/About Us are parallel bullet columns; larger centered legal line.

- V24: fixed popup links to open actual index.html pages and reduced modal bezels/padding.

- V25: Explore footer links now use normal in-page anchor scrolling and do not open popups.

- V26: added a right-aligned auto-updating carousel between capabilities and pricing. Add sequential PNGs to `carousel-images/` as `image1.png`, `image2.png`, etc.

- V27: replaced the plain carousel with a compact iPad-style frame and right-to-left sliding images.

- V28: changed the carousel to a smaller black tablet, matched for 1366x768-style screenshots, with edge-to-edge screen fill and tighter vertical spacing.

- V29: moved the iPad carousel beside the pricing heading, reduced vertical space, and smoothed right-to-left slide motion.

- V30: auto-fits the fixed-position typewriter text so every full phrase remains visible on desktop and mobile without changing the typing/erasing effect.

- V31: reduced the hero tablet/result card size and tightened vertical spacing across the landing page without removing content or changing animations.
